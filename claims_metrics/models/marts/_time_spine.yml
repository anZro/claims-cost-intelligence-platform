models:
  - name: metricflow_time_spine
    description: Daily grain time spine for MetricFlow time-based metrics.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        type: date
        granularity: day
